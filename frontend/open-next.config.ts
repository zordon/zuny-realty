import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // Add any specific OpenNext for Cloudflare options here if needed in the future.
  // For now, a minimal config is fine.
}); 